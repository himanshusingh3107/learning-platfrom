# LearnSpace Learning Platform

Simple Flask, HTML, CSS and SQLite project with admin, trainer and trainee roles.

Run:

```powershell
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:8000`.
